# Per-recording live state — generated by the replay harness +
# relay's log adapter. Never check in.
_live/
relay/fires.jsonl
relay/fires.*.jsonl

# Generated dataset CSVs — re-generate with the `generate.py` scripts.
datasets/falling_ball/falling_ball.csv
datasets/server_metrics/server_metrics.csv

# Local secrets for the relay's webhooks. NEVER check in.
.env
.env.*
secrets.json
# …but DO ship the template file so others know the shape.
!.env.demos.example
