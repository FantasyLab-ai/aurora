"""Pipeline entry scripts that ship inside the installable package.

The canonical dataset runner lives here so `pip install aurora-mcp`
carries the full analyze path; the repo-root ``scripts`` module keeps
a compatibility alias for existing workflows.
"""
