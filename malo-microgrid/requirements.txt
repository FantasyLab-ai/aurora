# Core (required)
requests>=2.28
networkx>=3.0

# Optional: metrics export and plots. The simulation degrades gracefully
# without them (falls back to the stdlib csv module and skips --plot).
pandas>=2.0
matplotlib>=3.7
