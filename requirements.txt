# Aurora — runtime dependencies (kept minimal; local-first promise)

# ---- core numerics ----
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3

# ---- time-series / stats ----
statsmodels>=0.14
ruptures>=1.1            # change-point detection

# ---- plotting ----
matplotlib>=3.7

# ---- web server ----
flask>=3.0
flask-cors>=4.0

# ---- multi-format dataset loader ----
pyarrow>=14.0            # parquet
openpyxl>=3.1            # xlsx

# ---- LLM narrative (optional, runs local Ollama by default) ----
requests>=2.31

# ---- runtime helpers ----
psutil>=5.9              # used by resource meters in the UI (future)
